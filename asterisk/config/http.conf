; Embedded HTTP server: serves the WSS transport. nginx terminates TLS and
; proxies /ws here, so we bind plain HTTP on the container network only.
[general]
enabled=yes
bindaddr=0.0.0.0
bindport=__HTTP_PORT__
; No TLS on this side: TLS is handled by the platform nginx in front.
enablestatic=no
